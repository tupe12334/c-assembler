import { join } from "path";

export const mainFolder = join(__dirname, "../../");
export const testingFolder = join(mainFolder, "testing");
export const expectationsFolder = join(testingFolder, "expectations");
